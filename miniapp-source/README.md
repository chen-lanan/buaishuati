# 不爱刷题业务源码 v2.0.7

当前解析器会先识别题库结构，再对严格/宽松边界解析候选进行评分选择；PDF 额外采用保守的多栏判断与页眉页脚清理，避免题干/选项缩进打乱题序。异常题支持置信度排序和筛选。OCR 暂不启用。

支持扩展名由 `services/docx-importer.js` 中的 `SUPPORTED_EXTENSIONS` 集中维护，Android 文件选择器会读取同一列表映射为 MIME 白名单。
